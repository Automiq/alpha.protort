syntax = 'proto3';

import "components.proto";

package alpha.protort.protocol.communication;

message Packet
{
    ComponentEndpoint source = 1;
    ComponentEndpoint destination = 2;
    bytes payload = 3;
}
