syntax = 'proto3';

package alpha.protort.protocol;

message Packet {

    uint32 id = 1;
    enum ComponentKind
    {
        Generator = 0;
        Retranslator = 1;
        Terminator = 2;
    }
    ComponentKind component = 2;
    bytes payload = 3;
}
